# P12.5 - Controlled Meteor Probe Verification

**Status: PARTIAL** — a real Meteor build was pinned locally and the probe key
was verified byte-for-byte against that build's own language resources. No
live client session could be run from this environment, so the full
build → client → response chain remains **UNAVAILABLE**. No production code,
probe definitions, keys, enforcement, or trust logic were changed. No Xaero
probe was touched.

## Test Environment

```text
Minecraft:       1.21.11 (pinned Meteor build targets 1.21.11; live client UNRECORDED)
Loader:          Fabric (pinned build is a Fabric mod; live client UNRECORDED)
Meteor version:  1.21.11-86 (mod id meteor-client, fabric.mod.json)
Meteor build SHA-256: 93390cfdb1030e47f116357ba421c90a9b1ca4832b6551c2dec1ade5eeb42e71
Additional mods: UNRECORDED live (local TofeMc instance also contains Xaero minimap/worldmap,
                 sodium, lithium, iris — consistent with the Player C description, but the
                 live Player C inventory was never recorded)
Xaero/map mods:  UNRECORDED live
Server version:  Paper/Folia 1.21.11 (fixtures present; no live player session run here)
VAntiCheat artifact SHA-256: 81f9d62b3a7dab3bf67d33a224ab0f3d22d8539364cede1a605704fd832614be
VAntiCheat version: 0.1.0-SNAPSHOT (target/vanticheat-0.1.0-SNAPSHOT.jar)
Build Java:      release 21 via Maven (shell JDK reports OpenJDK 26; artifact targets Java 21)
```

Pinned build copies (identical SHA-256): `/home/shadi/meteor-client-1.21.11-86.jar`
and `/home/shadi/.minecraft/mods/meteor-client-1.21.11-86.jar`.

## Probe

```text
probe ID:    meteor-client
mode:        METEOR
request key: key.meteor-client.open-gui
fallback:    [[NO_METEOR_CLIENT]] (auto-generated; no explicit fallback configured)
enabled:     true, selected manually AND automatically (P12.4, re-verified in JAR below)
```

## Actual Meteor Resource

Extracted directly from the pinned build (no internet, no guessing):

```text
Actual key:    key.meteor-client.open-gui
Source file:   assets/meteor-client/lang/en_us.json inside meteor-client-1.21.11-86.jar
Exact value:   "Open GUI"  (full file: open-gui, open-commands, category keys only)
Minecraft version: 1.21.11
Meteor version:    1.21.11-86
```

The configured probe key exists **exactly** in the real build. Key-validity
hypothesis (P12.4 option F as "wrong key") is **ELIMINATED** for Meteor
1.21.11-86.

End-to-end expectation against this exact build, by code trace:

```text
server sends Component.translatable("key.meteor-client.open-gui", "[[NO_METEOR_CLIENT]]")
  -> Meteor 1.21.11-86 resolves via its own en_us.json to "Open GUI"
  -> SignChangeEvent line "Open GUI"
  -> METEOR branch: not empty, not key+letter, != key, != fallback -> INITIAL DETECTED
  -> flagged -> CONFIRMATION re-run -> "Open GUI" again -> CONFIRMATION DETECTED
  -> final DETECTED -> confirmed positive -> KICK (non-trusted) / NONE (trusted)
```

Vanilla control by the same trace: no lang entry exists, client renders the
fallback `[[NO_METEOR_CLIENT]]`, evaluator `startsWith(fallback)` -> CLEAN.
No false positive is possible through this branch; locked by
`MeteorProbeIntegrityTest` (which asserts the real resolved value
`"Open GUI"` -> DETECTED).

## Manual Test

```text
START:          UNAVAILABLE (no live client session runnable from this environment)
PROBE EXECUTED: UNRECORDED live (selection code-verified: meteor-client in manual set)
RESPONSE:       UNRECORDED live
INITIAL:        UNRECORDED live
CONFIRMATION:   UNRECORDED live (runs iff flagged; code-verified)
FINAL:          UNRECORDED live (Player C historically reported CLEAN)
ENFORCEMENT:    UNRECORDED live
```

Required live command (unchanged): `/vac check PlayerC` on the server with the
current artifact, capturing
`Client probe result ... status=... mods=... session=...`.

## Confirmation

UNAVAILABLE live. Code path verified (P12.3/P12.4): flagged-only re-run,
`CONFIRMATION + DETECTED` required for a confirmed positive; initial-only
`DETECTED` never kicks (existing `EnforcementServiceTest`).

## Enforcement

UNAVAILABLE live. Semantics unchanged and deterministically tested:
non-trusted confirmed positive -> KICK; trusted confirmed positive -> NONE
with status kept DETECTED.

## Automatic Join

UNAVAILABLE live. `meteor-client` is present in the packaged
`auto-check.probes` list (JAR-verified this phase), so the join path uses
identical probe semantics to manual. Awaiting a live Meteor join.

## Control Client

UNAVAILABLE live. Expected by trace: vanilla/legitimate client returns the
fallback -> CLEAN, no kick. Must still be run once against the live server.

## What Player C's Historical CLEAN Now Means

With the key proven valid for 1.21.11-86, the remaining options are:

- Player C was not running Meteor 1.21.11-86 (different version without this
  key, Meteor absent, or belief wrong) — option H, now the leading hypothesis.
- The probe never executed or its response was lost on that session
  (would surface as ERROR/PROTECTED/SKIPPED in the new logging, not CLEAN).
- A CLEAN verdict from a completed session means every executed probe
  returned fallback/empty — i.e. the responding client did not resolve the
  Meteor key.

`UNKNOWN` is retained for the historical session (its responses were never
recorded), but the probe itself is no longer the suspect for that build.

## Final Classification

```text
UNAVAILABLE (live session) — with key existence VERIFIED from build resources
```

This is deliberately not `VERIFIED`: the phase definition requires the full
KNOWN BUILD -> REAL CLIENT -> REAL RESPONSE -> CONFIRMATION chain, and the
live links are still missing. It is equally not `EXPECTED LIMITATION` (the
signal exists in the pinned build) and not `BUG` (no defect demonstrated).

## Xaero

```text
Xaero Minimap/World Map probes remain disabled.
```

JAR-verified this phase: both definitions `enabled: false`, neither appears
in `auto-check.probes`, both excluded from manual execution. No Xaero probe
was created, enabled, or modified. `Xaero's Minimap != Meteor` and
`Xaero's Minimap != automatic cheat detection` preserved.

## Validation

```text
UNIT VERIFIED:             85 tests pass (incl. 5 MeteorProbeIntegrityTest)
CURRENT ARTIFACT VERIFIED: package PASS; meteor-client present+enabled+auto-listed;
                           xaero defs disabled and unlisted; no test classes, sources,
                           or velocity entries
LIVE VERIFIED:             none
UNVERIFIED:                live Meteor/manual/automatic/enforcement/control sessions;
                           meteor-client verification flag (see below)
```

```text
mvn clean test:    PASS (85 tests, 0 failures)
mvn clean package: PASS
git diff --check:  PASS
JAR inspection:    PASS
SHA-256:           81f9d62b3a7dab3bf67d33a224ab0f3d22d8539364cede1a605704fd832614be
                 (content-identical rebuild; zip timestamps vary per build — 85-test
                 full pass ran on the same tree immediately before repackaging)
```

Note: `verification: UNVERIFIED` on `meteor-client` was intentionally left
untouched. The flag is metadata-only (never read by detection/enforcement
logic — verified by grep), and per this phase's own definition of done only a
full live session may promote it. The existing `MeteorProbeIntegrityTest`
already pins the exact ID, mode, key, enabled state, and both selections, and
now additionally exercises the real resolved value `"Open GUI"`.
