# P3.7 - Final Stability Matrix & P3 Closure Report

## A. Paper Two-Player Isolation

**PASS**

Two real vanilla Minecraft 1.21.11 clients joined Paper 1.21.11 close
together as `CleanA` and `CleanB`. They received different detection sessions:

```text
CleanA: 36f028f0-21a2-4419-a2a8-c42ef1e58fa0 -> CLEAN
CleanB: e1e43128-a3f2-46da-bbbe-185086e1103c -> CLEAN
```

Each player had independent probe batches, evidence, completion, and no kick.
The live log is `/home/shadi/test-server-paper/server-p3.7-paper.log`.

## B. Mixed Clean/Modded Test

**UNVERIFIED**

The clean and Xaero-positive clients were verified independently in P3.6, but
not simultaneously in this phase. No simultaneous mixed-client claim is made.

## C. Join-Disconnect Race

**PASS**

`RaceB` joined Paper, automatic detection started, and the client was killed
during the first probe operation. The server recorded one
`outcome=DISCONNECTED`, no later batch, no kick, and no exception storm. The
changed module now explicitly cancels the active session and probe handle on
quit.

## D. Live Concurrency

**UNVERIFIED**

Two-player live isolation passed. The requested 4/8/16-player stress levels
were not completed.

## E. Concurrency Limit

**UNVERIFIED**

The configured limit remains `32`. Its admission behavior is covered by
deterministic tests, including skip-at-limit behavior, but a live 33-player
run was not performed.

## F. Folia Live Validation

**PASS**

Current VAntiCheat was deployed to Folia 1.21.11. The server reported
`platform=FOLIA`, accepted a real clean 1.21.11 client, completed all configured
probe batches, and returned `CLEAN` without enforcement. Folia shutdown also
disabled VAntiCheat cleanly.

The scheduler implementation routes operations through the existing abstraction:

```text
ENTITY  -> entity scheduler
REGION  -> region scheduler
GLOBAL  -> global region scheduler
ASYNC   -> async scheduler
```

Folia live positive detection was not performed.

## G. Cleanup Matrix

| Scenario | Status |
| --- | --- |
| Normal completion | PASS |
| Timeout | PASS, deterministic and prior Paper validation |
| Disconnect | PASS, live Paper race |
| Concurrent session | PASS, deterministic and two-player Paper live |
| Kick | PASS, prior Paper positive validation |
| Shutdown | PASS, Folia live shutdown |
| Direct world sign-leak inspection | UNVERIFIED |

Transport completion cancels timeout handles and restores temporary blocks through
the existing region cleanup path. No cleanup exception was observed in live logs.

## H. Session Reuse

**PASS**

After the `RaceB` disconnect session
`b8a1beec-b970-4b23-9abb-00bdef770a3e`, a later reconnect created a new
detection session:

```text
33e11fe9-721a-4cc9-b278-dd7a594b9b38
```

The reconnect completed independently as `CLEAN`.

## I. Enforcement Isolation

**PASS**

Clean players remained online during two-player Paper isolation and Folia
validation. The previously verified Xaero positive path remained confirmed-only
kick behavior. Disconnect produced no enforcement.

Simultaneous clean/modded enforcement isolation is **UNVERIFIED** because the
mixed run was not completed.

## J. Evidence Isolation

**PASS**

Paper live logs show distinct player UUID/session/probe records for `CleanA`
and `CleanB`. No response, timeout, result, or evidence record crossed players.

## K. Performance Sanity

**PASS** for completed live scenarios.

No busy loop, repeated config reload, blocking probe I/O, or unbounded behavior
was observed during the completed two-player Paper and one-player Folia runs.

Broad stress behavior remains **UNVERIFIED** with the larger player counts.

## L. Regression Tests

**PASS**

Added focused coverage for disconnecting an active detection session and
cancelling its active probe handle. Existing automatic scheduling, session,
concurrency, enforcement, and detection tests remain active.

## M. Final Test Count

```text
37 tests passed
0 failures
```

## N. Build

```text
mvn clean test       PASS
mvn clean package    PASS
git diff --check     PASS
```

## O. JAR Inspection

**PASS**

`target/vanticheat-0.1.0-SNAPSHOT.jar` contains:

- `plugin.yml`
- `config.yml`
- `client-detection.yml`
- automatic join coordinator and listener
- detection module and probe transport
- Paper/Folia scheduler classes
- enforcement service

No test artifacts or old `client`, `connection`, `policy`, or `protocol`
packages are present in the JAR.

## P. Files Added

- `docs/P3.7-FINAL-STABILITY-MATRIX.md`

## Q. Files Modified

- `src/main/java/site/vackstudio/vanticheat/detection/probe/CheckHacksClientDetectionModule.java`
- `src/main/java/site/vackstudio/vanticheat/platform/paper/AutomaticClientDetectionListener.java`
- `src/test/java/site/vackstudio/vanticheat/detection/probe/CheckHacksClientDetectionModuleTest.java`

## R. Files Removed

None in P3.7.

## S. Dependencies

No dependencies added.

## T. Remaining Limitations

- Simultaneous clean/ Xaero mixed-client run is unverified.
- Live 4/8/16-player concurrency is unverified.
- Live configured-limit saturation is unverified.
- Folia live positive detection is unverified.
- Direct world inspection for every temporary sign/barrier cleanup path is unverified.
- Bedrock/Geyser identification remains outside scope.

## U. P3 Final Status

**PARTIALLY VERIFIED**

The required Paper two-player isolation, Paper disconnect race, Paper session
reuse, Folia clean path, shutdown path, deterministic concurrency policy, and
regression suite are verified. P3 is not marked `CLOSED` because the requested
simultaneous mixed-client and larger live concurrency matrix was not completed.

## V. P4 Recommendation

Do not begin P4 automatically. Resolve the remaining mixed-client and live
stress validation gaps first, or explicitly accept them as operational test
limitations before changing scope.
