# Minimal VAntiCheat foundation configuration.
vanticheat:
  enabled: true
  debug: false
detection:
  enabled: true
enforcement:
  enabled: true
  confirmed-detection:
    message: "Cheating detected."
